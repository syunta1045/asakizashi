# 朝しるべ — AGENTS.md

朝の気分記録・習慣・日記アプリ（Expo）。**引き継ぎの本体は `HANDOFF.md`。**

> ⚠️ **このリポジトリは公開されている**（GitHub Pages でプライバシーポリシー・サポート・利用規約のページを配信しているため非公開にできない）。**事業の数字・収益・内部の方針・個人情報はここに書かない。**

## 現状
- 1.0.3 配信中（名前を「朝の気分記録・習慣・日記」の方向に変更／カテゴリをヘルスケア＆フィットネスへ／キーワード見直し／スクショ5枚／レビュー依頼を実装）
- 維持段階：壊れたら直す。機能は足さない

## 絶対にやらないこと
- **占いの語彙を戻さない**（App Store 審査ガイドライン 4.3(b) で繰り返しリジェクトされた経緯がある）

## コマンド（変更したら必ず自分で検証する）

セットアップ：`npm ci`

| 目的 | コマンド |
|---|---|
| まとめて検証 | `npm run check`（型チェック＋lint＋jest） |

クラウド版（Linuxコンテナ）ではiOSのビルド・シミュレータは動かない。検証は上のコマンドまで。
**人間の確認なしに実行しない**：`eas build`／`eas submit`／App Store Connect での提出・価格変更。
**秘密情報（`.env`、`*.p8`、APIキー）はリポジトリに無い。** 見つからなければ推測で作らずに止めて報告する。

## 踏みやすい罠
- カメラを使うなら expo-camera／expo-image-picker に `microphonePermission:false`（既定のマイク文言で5.1.1リジェクト）
- ストアの言語欄が英語だけになる → Expo標準の `locales` で `ja.lproj` を入れる
- expo-store-review は SDK56 では 56.0.3 に固定（56.0.4 はiOSビルドが落ちる）
- `EXPO_PUBLIC_*` はIPAを解凍すれば読める
- 他社アプリ名・商標をメタデータに入れない（規約2.3.7）

## 進め方
- 返答は短く。1ターンの作業は短く区切る
- 見た目の確認はHTMLで出す
